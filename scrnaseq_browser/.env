# scRNA-seq Browser - Local Configuration (Sin Docker)

# --- API Configuration ---
API_PORT=8000
API_BASE_URL=http://localhost:8000

# --- Shiny Configuration ---
SHINY_PORT=3838

# --- Cache Configuration ---
# Usar cache en memoria (no requiere Redis)
USE_REDIS=false
REDIS_URL=redis://localhost:6379/0

# --- Parquet Data Configuration ---
# Usar datos locales (más rápido, sin latencia S3)
#PARQUET_DATA_DIR=./parquet_data

# Para usar S3 (requiere AWS credentials), descomenta la siguiente línea:
PARQUET_DATA_DIR=s3://luis-scrnaseq-data/parquet_out

# --- Datasets Configuration ---
DATASETS_JSON='[{"id":"epdsc","name":"EPDSC Dataset","uri":"s3://luis-scrnaseq-data/parquet_out"},{"id":"parquet_bm","name":"Bone Marrow (Palantir)","uri":"s3://luis-scrnaseq-data/parquet_bm"}]'

# --- AWS Configuration (solo si usas S3) ---
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_here

# --- Performance Tuning ---
TILEDB_SOMA_THREADING=2
FASTAPI_WORKERS=1

